const userRole={
    user:2,
    admin:1
}

const userStatus = {
    approved:1,
    pending:2,
    rejected:3,
}

const payementStatus={
    paid:1,
    unpaid:2,
    cancel:3
}

const childType = {
    child:1,
    driver:2,
}

deviceStatus={
    active:1,
    inactive:2,
}

module.exports={payementStatus, userRole, userStatus, childType}