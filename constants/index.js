const userRole={
    admin:2,
    superAdmin:1
}

const userStatus = {
    approved:1,
    pending:2,
    rejected:3,
}

const childType = {
    child:1,
    driver:2,
}

const deviceStatus={
    active:1,
    inactive:2,
}

module.exports={userRole, userStatus, childType, deviceStatus}